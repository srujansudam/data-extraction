# SQLite SEE Runtime Placeholder

SQLite SEE source code, binaries, activation keys, and database keys are not included in this repository or release bundle.

The licensed client must compile or supply a SEE-enabled `sqlite3.dll` and place it beside `data-extraction.exe` in production. See `docs/sqlite_see_setup.md` for the setup and validation procedure.
