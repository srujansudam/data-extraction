# Domino Java Libraries

BOV/client IT must supply the approved `notes.jar` and `ncso.jar` files here for Java 8 compilation and runtime.

Do not commit or place proprietary Domino jars in source control or a generic release bundle.
